{{- if .Values.rbacHelpers.enable }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "chinflux-operator.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: {{ include "chinflux-operator.resourceName" (dict "suffix" "chinfluxrestore-admin-role" "context" $) }}
rules:
- apiGroups:
  - chinflux.chinflux.io
  resources:
  - chinfluxrestores
  verbs:
  - '*'
- apiGroups:
  - chinflux.chinflux.io
  resources:
  - chinfluxrestores/status
  verbs:
  - get
{{- end }}
